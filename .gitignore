# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Google OAuth credentials and tokens
client_secret.json
tokencache.json

# Rust / Tauri
target/
**/*.rs.bk
Cargo.lock
.pnpm-store

# Tauri build output
src-tauri/target/
src-tauri/WixTools

# MSI Installer logs
MSI*.LOG

# Build directories (but keep the ISO for production)
linux-build/*
!linux-build/photobooth.iso
!linux-build/.gitkeep

# VM exports (deprecated - we use lightweight ISO instead)
vm/*.ova
vm/*.vdi
vm/*.vmdk

# Claude settings
.claude/settings.local.json
